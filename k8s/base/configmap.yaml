kind: ConfigMap
apiVersion: v1
metadata:
  name: configmap
  namespace: petclinic
data:
  spring.datasource.username: postgres
  spring.datasource.driver-class-name: org.postgresql.Driver
  spring.datasource.url: jdbc:postgresql://postgres:5432/petclinic_service
